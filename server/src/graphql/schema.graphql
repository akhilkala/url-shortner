type Query {
   getAllUrls: [URL!]!
   getUrlByID(id:String):URL!
}

type Mutation {
   login(input:Auth):Res!
   register(input:Auth):Res!
   addURL(input:addURLInput):URL!
   deleteURL(id:String!):Res!
}

input Auth {
   email:String!
   password:String!
   token:String
}

type Res {
   error:String
   token:String
   message:String
}

input addURLInput {
   name:String!
   long:String!
   userID:String!
}

type URL {
   id:ID!
   name:String!
   long:String!
   short:String!
   mobileVisits:Int!
   desktopVisits:Int!
   user:User!
}

type User {
   username:String!
}


